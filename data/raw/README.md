Place original master JSON/CSV/XLSX here for ingest. Those files are gitignored when they contain a private home origin.

Committed: Israel_Essential_Apps_for_New_Olim.xlsx (already represented as APP-* records in the master).

After dropping a new master JSON, run `npm run ingest`.
